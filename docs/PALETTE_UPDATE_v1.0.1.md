# v1.0.1 配色更新の確認記録

確認日：2026-09-17（UTC）

## 変更

- 背景 `#f7faff`、盤面 `#e6effb`、主要操作 `#3165d9`、補助アクセント `#c3f3df`。
- 1〜9の駒を白・水色・ミント・黄・桃・ラベンダー系へ変更。数字と方向表示は濃い色を維持。
- 選択元は青い実線、着地先は緑の枠と結果バッジ、ヒント先は点線。配色に応じて操作状態の枠も更新。
- ダークモードはネイビー系へ統一。PWAのテーマ色はCSSの背景色に追従。
- PWAキャッシュを1.0.1へ更新し、配布用単体HTMLを再生成。
- `core.js`、保存キー、保存形式、レスポンシブのレイアウト寸法は変更していない。

## マージ前の確認

- `npm test`：27 / 27 PASS。既存のService Workerテストは現行1.0.1を保持し、旧1.0.0と0.9.0のみ削除するケースへ更新。
- `npm run build`：成功。
- `node --check app.js` / `node --check sw.js` / `git diff --check`：成功。
- 単体HTMLに現在のCSS・app.js・core.jsが含まれること、ゲームコアがベースコミットと同一であることを照合。
- sRGB相対輝度から主要テキスト・補助テキスト・盤面表示・ボタン・全9種の数字と方向表示のコントラスト比を計算。対象の最小値はライト **4.57:1**、ダーク **5.28:1**。方向表示は不透明度0.85を合成して算出。

## マージ前の未実施・限界

ローカルHTMLを開く操作がクラウドブラウザのURLポリシーに拒否されたため、今回のブラウザ描画・操作・スマートフォン幅の確認は未実施。実機iPhone/Safari確認も未実施。上の数値は指定した色ペアの計算であり、ページ全体のアクセシビリティ適合の証明ではない。

`docs/TEST_REPORT.md`と既存スクリーンショットはv1.0.0時点の履歴であり、今回の描画確認結果として扱わない。

## 公開後の確認と描画修正

v1.0.1はGitHub Actionsでテスト・公開とも成功。公開ページのChrome、幅1363pxでライト／ダークの背景・盤面色、設定切り替え、選択元と着地先のDOM状態を確認した。

公開画面で駒を選択すると一部の数字が背景の下に隠れる描画症状を観測したため、v1.0.2では各マスに独立した重なり順を設け、数字と方向表示のz-indexを明示した。寸法とゲームルールは変更していない。PWAキャッシュと単体HTMLも更新し、27件のテストとビルドは再度成功した。

v1.0.2の公開成功後にも、PWA更新操作で旧版1.0.1が表示される状態を観測。v1.0.3のService Workerインストールでは、各アセットを `Request(..., {cache: 'reload'})` で再取得し、HTTPキャッシュに残った旧版ファイルを新しいアプリキャッシュへ持ち込むことを防ぐよう変更した。既存テスト25にURLのスコープ・再取得モード・旧版キャッシュ削除の検査を維持／追加し、27件全通とビルド成功を確認した。
